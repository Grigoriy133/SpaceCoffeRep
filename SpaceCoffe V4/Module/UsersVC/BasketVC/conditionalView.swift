//
//  conditionalView.swift
//  SpaceCoffe V4
//
//  Created by Grigoriy Korotaev on 14.06.2023.
//

import Foundation

/*
let timerLabel = UILabel()
let startButtontapped = UIButton()
   
   var timer = Timer()
   var seconds = 60
   
   override func viewDidLoad() {
       super.viewDidLoad()
       view.backgroundColor = .white
       view.addSubview(timerLabel)
       view.addSubview(startButtontapped)
       timerLabel.text = "\(seconds)"
       startButtontapped.addTarget(self, action: #selector(startButtonTapped), for: .touchUpInside)
       startButtontapped.backgroundColor = .red
       createConstraint()
   }

@objc func startButtonTapped() {
   timer = Timer.scheduledTimer(timeInterval: 1, target: self, selector: #selector(updateTimer), userInfo: nil, repeats: true)
}

   @objc func updateTimer() {
       if seconds > 0 {
           seconds -= 1
           timerLabel.text = "\(seconds)"
       } else {
           timer.invalidate()
       }
   }
func createConstraint(){
   timerLabel.snp.makeConstraints() {
       $0.left.top.equalToSuperview().inset(100)
       $0.width.equalTo(200)
   }
   startButtontapped.snp.makeConstraints(){
       $0.left.equalToSuperview().inset(100)
       $0.top.equalTo(timerLabel.snp.bottom)
       $0.width.height.equalTo(100)
   }
}
*/
